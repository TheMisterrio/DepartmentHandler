language: python

python:
    - 3.8

install:
  - pip install -r requirements/dev.txt

script:
  - python -m department-app/tests discover
  - coverage erase
  - coverage run department-app/wsgi.py && coverage html
  - pylint --output-format=text department-app/wsgi.py

after_success:
  - codecov
